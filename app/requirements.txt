# MATEcito — dependencias
# Versiones fijadas para que el mismo código corra igual en todas las PC.
# Actualizar con criterio: probar los tests después de subir una versión.

fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
python-multipart>=0.0.9
phonenumbers>=8.13
openpyxl>=3.1
jellyfish>=1.0
oracledb>=2.0
cryptography>=42.0
mysql-connector-python>=8.3
user-scanner>=1.4

# SQL Server (diferido; descomentar cuando se active)
# pyodbc>=5.1
